process of updating remote repository whith local changes
-check status - (git status)

-git pull (pull changes from remote branch) or git pull origin <bramch name>

-git add filename.txt or  git add . (adds all file to staging)

    - git commit -m "commitmessage"
    - if already code exists in remote repo / e.g)someone made changes first upadate local  - git pull (gets updates)
    - git push  (pushes to current branch ) 
    - git push origin <branchname> (pushes to a specified branch)



check file status

    - git status

Restore changes that have been committed (not pushed )

    - git restore .

restore specific committe file

    - git restore --stage filename

check difference in changes

    - git diff

check hisory/previous commits

    -git log

check out all branches
- git branch -a 

Fetch all remote branches
- git fetch

